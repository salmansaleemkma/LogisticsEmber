import {
  flotChart
} from '../../../helpers/flot-chart';
import { module, test } from 'qunit';

module('FlotChartHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = flotChart(42);
  assert.ok(result);
});
